import styled from "@emotion/styled";
import { Box, Typography } from "@mui/material";

export const StyledLoginForm = styled(Box)({
  display: "flex",
  flexDirection: "column",
  gap: "20px",
  alignItems: "flex-start",
});

export const StyledResetTitle = styled(Typography)({
  paddingBottom: "20px",
});
